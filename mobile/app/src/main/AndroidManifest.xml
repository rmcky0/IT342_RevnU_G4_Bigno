<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_revnu_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_revnu_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".features.auth.ui.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".features.entry.ui.AddRecordActivity"
            android:exported="false" />
        <activity
            android:name=".features.settings.ChangePasswordActivity"
            android:exported="false" />
        <activity
            android:name=".features.restaurant.ui.RestaurantSetupActivity"
            android:exported="false" />
        <activity
            android:name=".core.ui.WebRedirectActivity"
            android:exported="false" />
        <activity
            android:name=".features.dashboard.ui.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".features.auth.ui.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".features.auth.ui.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".features.auth.ui.LoginActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>